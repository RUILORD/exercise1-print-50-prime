//Q:输出前50个素数
/*分析：先确定变量为1、要输出的数值x;2、计数到50 cnt;3、被除数i;4、isprime标志变量（假设）
再确定方法：嵌套循环
过程：外层循环数字-若内层确定是素数则打印，内层循环被除数找素数（这里确定素数的方法是用isprime=0或1来判断真假-不用==因为0/1不是真的数字）
最后注意语言的格式规范，如return0
*/
#include <studio.h>
int main() {  
    int x = 2; 
    int cnt = 0; 
    while (cnt < 50) { 
        int i;  
        int isprime = 1;               // 假设x是素数（这里的1指的是真）  
        for (i = 2; i < x; i++) {  
            if (x % i == 0) {  
                isprime = 0;           //0：假；表示此时的x不是素数
                break; 
            }  
        }  
        // 如果isprime仍为1，则x是素数  
        if (isprime == 1) {            /*这里以及上面都是==，不是= */
            cnt++; // 增加计数器  
			printf("%d\n", x); 
        }  
        x++; 
    }  
    return 0;  
}

/*在这里用while循环比for好些。因为不止需要cnt++,还需要x++。（如我第一次写时的：for（x=2;cnt<50;cnt++）这是忽略了x++的，就会导致陷入x=2的死循环）
while循环允许我们在循环体内部改变循环条件中使用的变量（在这个例子中是x），而for循环通常用于在循环开始前就已经知道循环次数的场景。*/

//注意每个语句的位置在花括号的哪里

如果有问题还请大神们多多指教！
